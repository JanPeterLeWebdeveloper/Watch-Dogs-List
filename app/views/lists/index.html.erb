<div class="container">
  <h1>My Movie App</h1>
    <!--Show all list-->
   <p><% @lists.each do |list| %>
      <%= link_to list.name, list_path(list) %>
   <% end %></p>
   
  <%= link_to "create a list", new_list_path   %>
</div>